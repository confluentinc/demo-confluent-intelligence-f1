# Self-service track

The self-service and workshop-fallback instructions are maintained together in
[`docs/backup/LOCAL-SELF-SERVICE.md`](../backup/LOCAL-SELF-SERVICE.md).

This pointer remains at the old path so existing links and bookmarks continue
to work. A filesystem symlink is intentionally avoided because GitHub does not
render linked Markdown as the destination document.
